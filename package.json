{
  "name": "autograde-lab-demo",
  "version": "0.1.0",
  "description": "AutoGrade Lab - MVP demo (simplified stack: Express + TS + in-memory storage, javac/java sandbox instead of Judge0)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "npx --yes tsx watch src/server.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "tsx": "^4.19.0",
    "typescript": "^5.5.3"
  }
}
